# Moorcheh API key — create one at https://console.moorcheh.ai/api-keys
# Free tier: 100K ops/month.
RIVERA_API_KEY=mch_xxxxxxxxxxxxxxxxxxxxxxxx

# Stable per-developer (or per-project) memory namespace. All skills in this
# repo share this agent, so context flows across every skill invocation.
RIVERA_AGENT_ID=skills-dev-profile

# How many memories to inject before a skill runs (default 8).
RIVERA_RECALL_LIMIT=8

# Optional floor on Rivera's information-theoretic retrieval (ITS) score.
# Leave unset: Rivera already returns only relevant results, and ITS scores
# live on a small, non-cosine scale (top hits are often ~0.1-0.2), so a naive
# 0-1 floor would discard everything. Only set this if you know the scale.
# RIVERA_MIN_SIMILARITY=
